package com.github.firulapp.repository;

import com.github.firulapp.domain.HelpPage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HelpPageRepository extends JpaRepository<HelpPage, Long> {
}
